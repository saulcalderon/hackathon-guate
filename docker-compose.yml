services:
  db:
    image: postgres:16.8
    environment:
      POSTGRES_USER: findr
      POSTGRES_PASSWORD: findr
      POSTGRES_DB: findr
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
